#include <iostream>
#include <string>
#include <vector>
#include <cctype>

using namespace std;

int solution(string& str) {
    for (string::iterator it = str.rbegin(); it != str.rend(); it++) {

    }
}

int main(void) {
    string str;

    cin >> str;
    cout << solution(str) << '\n';
}
