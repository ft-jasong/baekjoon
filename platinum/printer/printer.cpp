#include <string>
#include <algorithm>
#include <vector>

using namespace std;

int solution(vector<int> priorities, int location) {
    int answer = 0;
	vector<int> v_loc;

	for (int i = 0; i < priorities.size(); i++)
		v_loc.push_back(i);
	while (vector::iter)
	{
	}
    return answer;
}